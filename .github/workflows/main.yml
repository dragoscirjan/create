name: "Create CICD"

on:
  push: {}
  pull_request:
    branches: [main]

jobs:
  publish:
    name: "Create Node Module"
    uses: templ-project/create/.github/workflows/create-node-module.yml@main
    secrets: inherit
